import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Forge AI Workflow Studio",
    template: "%s | Forge",
  },
  description:
    "Visual IDE for building and deploying MCP-first agentic AI workflows. No code required.",
  keywords: ["AI", "workflow", "MCP", "LangGraph", "automation", "agents"],
  openGraph: {
    title: "Forge AI Workflow Studio",
    description: "Visual IDE for MCP-first agentic AI",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
